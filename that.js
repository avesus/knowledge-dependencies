const definition = require('definition');
const which = require('which');

console.log(`
The word 'that' is used in a definition.
	If the part after 'that' can be safely removed
	without changing the meaning of the definition
	(it's an explainer rather than definer),
	use the word 'which' instead.
`);

