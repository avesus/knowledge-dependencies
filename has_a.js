const is_a = require('is_a');

console.log(`
'Has a' is a one-to-many relation. (Possible) inverse of 'is a'.
	Is a weak connection, because the term is not parametrized.
	So the entire statement stays abstract (parametrizable by itself).
`);
