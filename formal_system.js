const that = require('that');
const language = require('language');

console.log(`
Formal system
	is a language
		over some alphabet
			of symbols
	that has
		a set of axioms
		and
		a set of inference rules
		that distinguish some of the strings in the language as theorems.
`);
