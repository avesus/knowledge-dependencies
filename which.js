const definition = require('definition');
const that = require('that');

console.log(`
The word 'which' should not be used in a definition,
	because it dilutes it with unnecessary details.
	If the part after 'which' can't be safely removed
	without changing the meaning of the definition
	use the word 'that' instead.
`);

