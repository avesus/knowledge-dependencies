const is_a = require('is_a');
const has_a = require('has_a');

const formal_system = require('formal_system');

const well_defined = require('well_defined');
const syntax = require('syntax');
const syntax = require('semantics');
const syntax = require('proof-theory');

console.log(`
Logic is a formal system that:
  - has a well-defined syntax;
  - has a well-defined semantics;
  - has a well-defined proof-theory.
`);


